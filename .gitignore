# credenciais NUNCA vão para o repositório (configurar em Settings > Secrets
# no Streamlit Cloud). A pasta inteira fica de fora, para pegar também os
# backups que gerar_senha.py cria ao trocar uma senha.
.streamlit/
__pycache__/
*.pyc

# atalho temporário do capturar_prints.py (abre o painel sem login) —
# existe só durante a captura e nunca pode ir para o repositório
_prints_app.py
